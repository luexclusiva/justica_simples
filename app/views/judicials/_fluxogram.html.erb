<div class="tab-pane fade" id="fluxograma" role="tabpanel" aria-labelledby="proposals-tab">
  <div class="fluxogram col-sm-12 col-md-6 offset-md-3">


      <div class="rounded-pill petitoria mt-3 d-flex align-items-center <%= @phase[1] == 0 ? "phase-yellow" : "phase-grey" %>">
        <p class="p-2">
          Fase de Apresentação dos pedidos e justificativas
        </p>
      </div>

      <div id="<%= "atual_" if @phase[1]==1 %>pointer"></div>  

      <div class="conciliatoria mt-3 d-flex align-items-center <%= if @phase[1] == 1 then "phase-yellow" elsif @phase[1] > 1 then "phase-grey" end %>">
        <p class="p-2">
          Fase de tentativa de acordo
        </p>
      </div>

      <div id="<%= "atual_" if @phase[1]=2 %>pointer"></div>  

      <div class="provas mt-3 d-flex align-items-center <%= if @phase[1] == 2 then "phase-yellow" elsif @phase[1] > 2 then "phase-grey" end %>">
        <p class="p-2">
          Fase de apresentação de provas
        </p>
      </div>

        <div id="<%= "atual_" if @phase[1]==3 %>pointer"></div>  

      <div class="rounded-pill decisoria mt-3 d-flex align-items-center <%= "phase-yellow" if @phase[1] == 3 %>">
        <p class="p-2">
          Fase de decisão do processo
        </p>
      </div>

  </div>
</div>
