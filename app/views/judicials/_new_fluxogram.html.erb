
  <div class="d-flex justify-content-center mt-5" >
    <h5>
      Em que fase estou?
    </h5>
  </div>
<div class="fluxogram">
  <div class="flux text-center border-bottom">

    <ol class="arrows mb-1 pl-0">
      <li class="color-gray <%= @phase[1] == 0 ? "color-orange" : "color-gray" %>"><a href="#" class="clickTab" data-toggle='tab'>
        <span class="fa-stack <%= @phase[1] == 0 ? "destaque" : "fora-destaque" %>">
            <!-- The icon that will wrap the number -->
            <span class="far fa-circle fa-stack-2x <%= @phase[1] == 0 ? "text-muted" : "text-light" %>"></span>
            <!-- a strong element with the custom content, in this case a number -->
        </span>
      </a></li>
      <li class="<%= if @phase[1] == 1 then "color-orange" elsif @phase[1] > 1 then "color-gray" end %>"><a href="#" class="clickTab" data-toggle='tab'>
          <span class="fa-stack <%= @phase[1] == 1 ? "destaque" : "fora-destaque" %>">
              <!-- The icon that will wrap the number -->
              <span class="far fa-handshake fa-stack-2x <%= @phase[1] == 1 ? "text-muted" : "text-light"  %>"></span>
              <!-- a strong element with the custom content, in this case a number -->
          </span>
      </a></li>
      <li class= "<%= if @phase[1] == 2 then "color-orange" elsif @phase[1] > 2 then "color-gray" end %>"><a href="#" class="clickTab" data-toggle='tab'>
        <span class="fa-stack <%= @phase[1] == 2 ? "destaque" : "fora-destaque" %>">
          <span class="far fa-file-alt fa-stack-2x <%= @phase[1] == 2 ? "text-muted" : "text-light"  %>"></span>
          </span>
      </a></li>
      <li class= "<%= "color-orange pr-0 " if @phase[1] == 3 %>"><a href="#" class="clickTab" data-toggle='tab'>
        <span class="fa-stack <%= @phase[1] == 3 ? "destaque" : "fora-destaque" %>">
          <span class="fas fa-gavel fa-stack-2x <%= @phase[1] == 3 ? "text-muted" : "text-light"  %>"></span>
          </span>
      </a></li>
    </ol>
  </div>
  <div class="mt-3">
    <% case @phase[1] %>
      <% when 0 %>

      <h5 class="text-center mt-1">
        Apresentação dos pedidos e justificativas
      </h5>
      <p>Apresente seu pedido para o juiz</p>

      <% when 1 %>
      <h5 class="text-center mt-1">
        Tentativa de acordo
      </h5>
      <p>Fique atento às datas de audiências. Não falte!</p>
      <% when 2 %>
      <h5 class="text-center mt-1">
        Apresentação de provas
      </h5>

      <p>Prepare os documentos, chame as testemunhas.</p>

      <% else %>
      <h5 class="text-center mt-1">
        Decisão do processo
      </h5>

      <p>Sua decisão está chegando! Se não concordar, pode recorrer.</p>
    <% end %> 
  </div>
  <div class="text-right">
    <small>
      <a data-toggle='tab' class="clickTab" href='#'>Saiba mais sobre as fases</a>

    </small>
  </div>
</div>
