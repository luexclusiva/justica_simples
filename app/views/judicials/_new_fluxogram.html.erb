<div class="d-flex justify-content-center mt-5">
  <h5>
    Em que fase estou:
  </h5>
</div>
<div class="fluxogram">
  <ol class="arrows mb-1">
     <li class="color-gray <%= @phase[1] == 0 ? "color-orange" : "color-gray" %>"><a href="#">
      <span class="fa-stack <%= @phase[1] == 0 ? "destaque" : "fora-destaque" %>">
          <!-- The icon that will wrap the number -->
          <span class="far fa-circle fa-stack-2x <%= @phase[1] == 0 ? "text-light" : "text-muted" %>"></span>
          <!-- a strong element with the custom content, in this case a number -->
          <strong class="fa-stack-1x <%= @phase[1] == 0 ? "text-sucess" : "text-danger" %>">
            1
          </strong>
      </span>
     </a></li>
     <li class="<%= if @phase[1] == 1 then "color-orange" elsif @phase[1] > 1 then "color-gray" end %>"><a href="#">
        <span class="fa-stack <%= @phase[1] == 1 ? "destaque" : "fora-destaque" %>">
            <!-- The icon that will wrap the number -->
            <span class="far fa-handshake fa-stack-2x <%= @phase[1] == 1 ? "text-muted" : "text-light" %>"></span>
            <!-- a strong element with the custom content, in this case a number -->
            <strong class="fa-stack-1x <%= @phase[1] == 1 ? "text-sucess" : "text-danger" %>">
              2
            </strong>
        </span>
     </a></li>
     <li class= "<%= if @phase[1] == 2 then "color-orange" elsif @phase[1] > 2 then "color-gray" end %>"><a href="#">
      <span class="fa-stack <%= @phase[1] == 2 ? "destaque" : "fora-destaque" %>">
        <span class="far fa-file-alt fa-stack-2x <%= @phase[1] == 2 ? "text-light" : "text-muted" %>"></span>
            <strong class="fa-stack-1x <%= @phase[1] == 2 ? "text-danger" : "text-sucess" %>">
              3
            </strong>
        </span>
     </a></li>
     <li class= "<%= "color-orange" if @phase[1] == 3 %>"><a href="#">
      <span class="fa-stack <%= @phase[1] == 3 ? "destaque" : "fora-destaque" %>">
        <span class="fas fa-gavel fa-stack-2x <%= @phase[1] == 3 ? "text-light" : "text-muted" %>"></span>
            <strong class="fa-stack-1x <%= @phase[1] == 3 ? "text-danger" : "text-sucess" %>">
              4
            </strong>
        </span>
     
     
     </a></li>
  </ol>
  <div>
    <% case @phase[1] %>
      <% when 0 %>
      <h5 class="border-bottom text-center"> 
        Apresentação dos pedidos e justificativas
      </h5>
      <p>Por enquanto nada a se preocupar</p>
      <% when 1 %>
      <h5 class="border-bottom text-center"> 
        Tentativa de acordo
      </h5>
      <p>Fique atento às datas de audiências</p>
      <% when 2 %>
      <h5 class="border-bottom text-center"> 
        Apresentação de provas
      </h5>
      <p>Prepare os documentos</p>
      <% else %>
      <h5 class="border-bottom text-center"> 
        Decisão do processo
      </h5>
      <p>Nessa fase o juíz finalmente vai decidir algo</p>
    <% end %> 
  </div>
  <div class="text-right">
    <small >
      <a  href="#">Saiba mais sobre as fases</a>
    </small>
  </div>
</div>

